# Project Agents & Task List

This file is used by the development agent (GitHub Copilot) for planning and tracking tasks needed to bring the project into compliance with the Westcoast Education specification.

## 📋 Project Summary

The repository is a simple TypeScript-based single‑page application for a fictitious car gallery called **WestCoast Cars**. It follows a modular folder structure: data models, interfaces, HTTP client, and page-specific code. Key features:

- **TypeScript for all logic** (`.ts` files) with type definitions (`interfaces` and `models`).
- **Static JSON data** (`data/courses.json`, `data/classrooms.json`) served via a lightweight HTTP client abstraction.
- **Pages**: gallery, vehicle details, about, contact, each with its own HTML, CSS, and logic.
- **Unit tests** written in plain JavaScript for DOM interaction (`.test.js` files).
- **CSS modules** for layout and visual styling under `src/assets` and page-specific CSS.
- **Build output** in `dist/` generated by a TypeScript compiler and possibly bundler.

The project structure mirrors the pattern outlined in the specification: clear separation of concerns, use of interfaces, environment configuration, and testing.

## ✅ Agent Todo List

1. **Review specification from PDF**
   - Read and extract requirements (data handling, pages, tests, build scripts).
2. **Ensure TypeScript configuration**
   - Validate `tsconfig.json` settings (`esModuleInterop`, `strict`, `outDir`, etc.).
3. **Implement/verify HTTP client and typings**
   - Confirm `IHttpClient` and `httpClient.ts` comply with spec (GET, error handling).
4. **Data model alignment**
   - Check `vehicle.ts`, `post.ts`, `enums.ts` against requirements.
5. **Page features and tests**
   - Gallery page: load all courses, create DOM elements, unit test coverage.
   - Vehicle details page: parse query string, display details, unit tests.
   - Contact page: form validation and submission behavior, basic unit tests.
   - About page: static content, styling.
6. **Styling and responsiveness**
   - Verify CSS rules match specification (grid layouts, nav styles, spinner).
7. **Environment setup**
   - Ensure `config/env.ts` exports correct values and is used by HTTP client.
8. **Build & lint**
   - Add or confirm scripts in `package.json` (`build`, `test`, `lint`).
   - Install/verify development dependencies (`typescript`, `jest` or similar).
9. **Documentation** ✅
   - Create README with usage instructions and architecture overview. (Completed 2026-03-05)
10. **Final verification**

- Run all tests, build the app, and open in browser to ensure functionality.

> _Tasks above may be expanded into more granular subtasks as development progresses._

---

_Agent notes:_ keep the same project structure and code style when adding features. Follow the Westcoast Education guidelines strictly (use TypeScript, modules, testing, environment variables, etc.).
