{
  "name": "westcoast-education",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc --watch",
    "test": "jest",
    "lint": "tsc --noEmit",
    "server": "json-server --watch data/courses.json"

    
  },

  

  "devDependencies": {
    "@types/jest": "^29.0.0",
    "jest": "^29.0.0",
    "jest-environment-jsdom": "^30.2.0",
    
    "ts-jest": "^29.0.0",
    "typescript": "^4.9.0"


  }
}
